//! This module describes how the [`ChatPlugin`] is plugged into the Bevy engine.

use crate::ChatPlugin;
use bevy::prelude::Plugin;

impl Plugin for ChatPlugin {
    fn build(&self, app: &mut bevy::prelude::App) {
        todo!()
    }
}
